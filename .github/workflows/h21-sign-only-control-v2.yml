name: H21 sign-only surface control v2

on:
  workflow_dispatch:
  push:
    branches:
      - research/hatter-sol-world-response-rubik-machine
    paths:
      - "papers/HATTER-SOL/21-INTERWORLD-PRIMALITY-TOMOGRAPHY/tools/sign_only_surface_control_v2.py"
      - ".github/workflows/h21-sign-only-control-v2.yml"

jobs:
  lab04:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run sign-only control v2
        working-directory: papers/HATTER-SOL/21-INTERWORLD-PRIMALITY-TOMOGRAPHY
        run: python3 tools/sign_only_surface_control_v2.py --bits 19 --out-dir lab04_out
      - uses: actions/upload-artifact@v4
        with:
          name: h21-lab04-sign-only-control-v2
          path: papers/HATTER-SOL/21-INTERWORLD-PRIMALITY-TOMOGRAPHY/lab04_out/
