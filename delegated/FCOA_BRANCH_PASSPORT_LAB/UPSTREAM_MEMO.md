# FCOA Branch Passport Laboratory — Upstream Memo

**Rounds:** P0–P1  
**Audience:** main Commander Sol scientific director

## U1-01 — G4 independent small-case automorphism gate PASSED

The P0 audit found that `verify_g4.py` did not independently enumerate its displayed definedness-group orders. The passport laboratory has now supplied a separate exhaustive enumerator that uses no expected group formula.

For `N=3,4,5,6`, direct enumeration of all base-carrier permutations gives:

| N | G4-C `|Aut(D|X)|` | G4-C full | G4-A `|Aut(D|X)|` | G4-A full |
|---:|---:|---:|---:|---:|
| 3 | 2 | 2 | 4 | 1 |
| 4 | 6 | 2 | 12 | 1 |
| 5 | 24 | 2 | 48 | 1 |
| 6 | 120 | 2 | 240 | 1 |

This independently confirms the mandatory small cases of

`Aut(D_4C | X_N) ~= S_{N-1}`,

`Aut(star_4C) ~= C2`,

`Aut(D_4A | X_N) ~= C2 x S_{N-1}`,

`Aut(star_4A) = 1`.

The resulting independently computed VRI values agree with

`VRI(G4-C) = (N-1)!/2`,

`VRI(G4-A) = 2(N-1)!`.

No small-case counterexample was found. In particular the `N=3` edge case is confirmed: `S_2=C_2`, so `VRI(G4-C)=1`.

**Recommendation:** the specific computational-evidence objection U0-01 is closed. This is not by itself authority to promote G4: retain the upstream requirement for proof-level hostile audit and sorting/anonymity reconciliation.

## U1-02 — G3 hostile-audit repair independently reproduced

The same formula-blind enumerator returns for `N=3..6`:

- G3-S: definedness/full = `2/2`;
- G3-C: definedness/full = `2/2`;
- G3-A: definedness/full = `4/1`.

For G3-A the definedness group is generated by boundary transposition and generic path reversal. Thus the earlier correction

`Aut(D_A | X_N) ~= C2 x C2`

has independent finite regression protection.

## U0-02 — General methods result: FCOA needs a genuinely multi-coordinate invariant passport

The audited G3 transitions demonstrate nonredundancy of the principal structural coordinates.

- G3-S and G3-C: same domain, same Association Spectrum, same automorphism-group size, different commutation geometry.
- G3-C and G3-A: same commutation count, different full-operation rigidity and different value-erasure behavior.

Therefore no passport based only on domain + spectrum + `|Aut|` is adequate. At minimum branch comparison must separately carry domain geometry, value-fiber partition, commutation locus, Association Spectrum, full-operation automorphisms, active-sort definedness automorphisms, one-sorted caveats, translation profiles, output orbits, and erasure/recoverability data.

## Next upstream-relevant gate

The next result worth sending upstream should be one of:

1. a G4 hostile-audit contradiction/repair;
2. a proof that a proposed passport coordinate is redundant or independent of the others;
3. a generally useful branch-diff theorem;
4. a new counterexample showing that two branches collide on all currently mandatory invariants except one.

Routine tooling improvements stay inside the passport laboratory.
