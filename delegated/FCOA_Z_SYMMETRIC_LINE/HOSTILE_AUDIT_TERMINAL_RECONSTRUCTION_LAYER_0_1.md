# FCOA-Z — Hostile Audit of the Terminal Reconstruction Layer 0.1

**Date:** 2026-08-31  
**Verdict:** PASS WITH ONE REPAIRED SCOPE DEFECT  
**Publication threshold:** REACHED AFTER 0.2 CORRECTIONS  
**Dimensional gate:** `c_coord = 0`

---

## 1. Audited package

This audit covers:

1. `TERMINAL_LIFT_PROFILE_AND_RELATIVE_RECONSTRUCTION_0_1.md`
2. `TERMINAL_OPACITY_AND_UNIFORMITY_INDEPENDENCE_0_1.md`
3. `FINITE_CHANNEL_COUPLING_CLASSIFICATION_0_1.md`
4. `TERMINAL_COST_MEMORY_DUALITY_0_2.md`
5. `SHARE_SPLIT_SELECTOR_AUDIT_0_2.md`

The superseded files

- `TERMINAL_COST_MEMORY_DUALITY_0_1.md`
- `SHARE_SPLIT_SELECTOR_AUDIT_0_1.md`

are not publication sources.

---

## 2. Real defect found and repaired

The original 0.1 cost/memory note claimed that for each fixed finite window the unique global minimizer/maximizer was global `share`/`split`.

This was too strong because profile bits outside a fixed window are invisible to that window.

The corrected statements are:

- on a fixed window, the all-share visible restriction uniquely minimizes cost;
- on a fixed window, the all-split visible restriction uniquely maximizes branch memory;
- global `share` uniquely minimizes cost simultaneously on all finite windows;
- global `split` uniquely maximizes branch memory simultaneously on all finite windows.

The correction is incorporated in versions 0.2 and does not change the exact cost-memory identity.

---

## 3. Terminal-orbit index audit

The canonical minimal signed-M0 table has exactly the following terminal-producing positive source orbits:

\[
(+,n),\quad n\ge1,
\]

\[
(*,n),\quad n\ge2,
\]

\[
(\times,n),\quad n\ge2.
\]

No `otimes` terminal orbit exists at \(n=1\), consistent with the audited exceptional cells

\[
x_1^\sigma\otimes x_0,
\qquad
x_1^\sigma\otimes x_1^\sigma
\]

remaining undefined.

**Verdict:** PASS.

---

## 4. Profile classification audit

Within the explicitly defined orbitwise canonical lift class, every terminal-producing reflection orbit independently admits exactly two choices:

- one reflection-fixed shared output;
- one fresh reflected two-cycle.

Different channel/depth indices cannot be identified by a typed signed-M0 isomorphism because:

1. `oplus` reconstructs root and radial depth up to global branch reflection;
2. operation symbol plus source-cell pattern distinguishes the `+`, `*`, and `times` terminal channels;
3. fixed-point versus two-cycle type is invariant under terminal reflection isomorphism.

Therefore

\[
F_\epsilon\cong F_\eta
\iff
\epsilon=\eta.
\]

Since the index set is countably infinite,

\[
|\{0,1\}^{\mathcal I}|=2^{\aleph_0}.
\]

**Verdict:** PASS, strictly scoped to the orbitwise canonical lift class.

---

## 5. Common base-shadow fibre audit

Every profile changes only terminal fibers and terminal-valued components. All base points, base-valued operation cells, the root, shift, and reflection are unchanged.

Thus all profiles have the same base reduct, while the fibre contains \(2^{\aleph_0}\) pairwise nonisomorphic expansions.

The derived statement that finite strings over a fixed finite alphabet cannot code all such expansions is valid because the set of all finite strings is countable.

**Verdict:** PASS.

---

## 6. Finite-schema reconstruction audit

The signed-M0 base-base table is generated by seven active recursive schema families plus a default-undefined rule:

1. root-left `oplus` identity;
2. root-right radial `oplus` contraction;
3. `oplus` diagonal terminal channel;
4. root-left `otimes` absorption;
5. `otimes` root-escape terminal channel;
6. same-branch local-unit `otimes` law;
7. `otimes` diagonal terminal channel.

The schema correctly retains all exceptional undefined cells and all mixed-sign `UNDEF` cells.

Given the reconstructed signed line and a terminal profile, it determines the full signed-M0 expansion up to typed isomorphism.

This is recursive structural synthesis, not a claim of parameter-free first-order definability.

**Verdict:** PASS.

---

## 7. Relative provenance audit

The previously published provenance no-go remains valid for the bare generated classical algebra.

The later relative reconstruction theorem adds extra source-theory information: the signed-M0 law schema and profile.

Thus there is no contradiction between:

\[
\text{bare shadow does not recover primitive provenance}
\]

and

\[
\text{bare shadow + fixed source schema + profile uniquely reconstructs the source class}.
\]

**Verdict:** PASS WITH EXPLICIT SCOPE DISTINCTION.

---

## 8. Uniformity-independence audit

Because terminal outputs do not re-enter primitive operations and no cross-terminal relation is present, choices on distinct terminal-producing orbits do not interact.

Hence every binary profile is realizable and every channel-uniform triple

\[
(b_+,b_*,b_\times)\in\{0,1\}^3
\]

is realizable.

Therefore current signed-M0 terminal-opacity axioms force neither radial uniformity nor cross-channel equality nor global `share`/`split`.

**Verdict:** PASS.

---

## 9. Equality-coupling classification audit

For a finite graph \(G\) on channel labels, equalities on edges force profile bits to be constant exactly on connected components.

Therefore

\[
N(G)=2^{c(G)}.
\]

For three channels:

\[
8\to4\to2
\]

under zero, one, and two independent equality edges.

Pure equality constraints preserve global bit complement and therefore cannot choose between global `share` and global `split`.

The equality-plus-anchor unit-cost optimum `3` is valid only in that declared constraint language.

**Verdict:** PASS WITH SCOPE CORRECTLY STATED.

---

## 10. Cost-memory duality audit

For every finite window, each split orbit contributes exactly:

- one new reflected terminal output;
- one branch-distinguishable terminal orbit.

Thus pointwise

\[
C_N(\epsilon)=M_N(\epsilon).
\]

The exact orbit count

\[
Q_N=3N-2
\]

for \(N\ge2\) is correct.

The repaired all-window extremal statements are correct.

Plain infinite cardinality is correctly rejected as a selector because both endpoint terminal universes are countably infinite.

**Verdict:** PASS AFTER 0.2 REPAIR.

---

## 11. Selector audit

The previously proved finite-window automorphism groups give:

\[
Aut(W_N,\otimes)_{split}\cong S_{N-1}\wr C_2,
\]

\[
Aut(W_N,\otimes)_{share}\cong S_{N-1}\times C_2.
\]

Thus `share` is more rigid in the `otimes` reduct for \(N\ge3\).

But with both operations retained,

\[
Aut(W_N,\oplus,\otimes)\cong C_2
\]

for both lifts, so full-operation rigidity is a nonselector.

Terminal reflection is trivial on global `share` and nontrivial, hence faithful as a \(C_2\)-action, on global `split`.

Therefore the selector family genuinely conflicts rather than secretly selecting one canonical lift.

**Verdict:** PASS.

---

## 12. Prior-art boundary

The following surrounding mathematics is classical and must not be claimed as new:

1. reducts and expansions of structures;
2. definitional and model-unique extensions;
3. Beth-style implicit/explicit definability;
4. finite covers and expansions in model theory;
5. propagation of equality constraints along connected components;
6. spanning-tree bounds;
7. elementary cost/Pareto counting.

Relevant classical boundary literature includes work on definitional/model-unique extensions and finite covers of structures. The publication must present these as conceptual context only.

The FCOA-specific theorem package is the exact computation for the audited signed-M0 source:

\[
2^{\aleph_0}\to8\to4\to2\to1
\]

under explicit reconstruction regularity/coupling assumptions, together with the exact finite-window terminal output-cost/branch-memory law and the conflicting `share/split` selector audit.

---

## 13. Novelty discipline

Do not claim novelty for the abstract statement that one reduct can have many expansions.

Potentially publishable FCOA-specific contribution is the conjunction:

1. a previously constructed one-dimensional partial algebra has a classical shadow that reconstructs its carrier but erases terminal lift geometry;
2. the erased terminal layer is classified exactly by a countable binary profile;
3. the common base-shadow fibre has continuum many canonical terminal expansions;
4. explicit regularity reduces the ambiguity to 8 channel profiles;
5. finite channel coupling gives an exact `8 -> 4 -> 2 -> 1` reconstruction-cost law;
6. each split terminal orbit costs exactly one new output and restores exactly one unit of branch-sensitive terminal memory;
7. natural structural selectors disagree on whether `share` or `split` should be preferred.

---

## 14. Dimensional audit

No theorem introduces:

- a second unbounded coordinate;
- a plane;
- an independent iterable generator;
- terminal re-entry transport.

Finite channel graphs and finite terminal fibers are internal finite structure only.

\[
\boxed{c_{coord}=0.}
\]

**Verdict:** PASS.

---

## 15. Publication readiness

After the 0.2 scope correction, the post-publication terminal reconstruction layer is mathematically closed enough for a separate article.

### Final verdict

\[
\boxed{\text{PUBLICATION THRESHOLD REACHED}.}
\]

Recommended paper nucleus:

\[
\boxed{
\text{continuum expansion fibre}
\to
\text{relative reconstruction compression}
\to
\text{uniformity independence}
\to
\text{finite coupling law}
\to
\text{cost-memory duality}
\to
\text{selector conflict}.
}
\]

No further theorem should be added before publication unless it repairs a discovered error.