# SOL-LOCALITY

**Scientific direction:** AQFT / microcausality / geometry-conditioned commutation  
**Branch:** `director/fcoa-z-symmetric-line`  
**Status:** RESEARCH PHASE COMPLETE / LOCALITY CEILING REACHED  
**Verdict:** `FORMAL EMBEDDING` (orthogonality/locality shadow only)  
**Line status:** `1D-CLOSED` + `UNDERDETERMINED ACROSS OUTPUT POLICIES`  
**Publication status:** `READY FOR APPLIED-DIRECTIONS SYNTHESIS / NOT STANDALONE PHYSICAL PUBLICATION`

## Final result

The FCOA-Z signed line supports a mathematically clean relation-locality abstraction:

\[
\boxed{L_{\pm}(x,y)\Longrightarrow \mathcal C_{\widehat\oplus}(x,y)=EQ}
\]

for conservative one-dimensional mixed-sector extensions, while the full operation remains globally noncommutative.

The literal AQFT identification is rejected: opposite FCOA branches are not spacelike-separated regions, and the published FCOA core has no Lorentzian causal structure, native local observable net, covariance, or state space. The surviving bridge is the more general orthogonal-category pattern:

\[
\boxed{\text{an independently defined geometric relation selects pairs whose algebraic images commute}.}
\]

## Three completed strikes

### v0.1 — locality construction and first underdetermination theorem

A branch-locality predicate, orthogonal-category shadow, algebra-valued locality envelope, and explicit locality-compatible FCOA mixed completion were constructed.

An infinite family of reflection-equivariant, conservative, base-valued, locality-compatible laws \(N_j\) proves that locality alone is not a mixed-value generator.

### v0.2 — selector no-go and output-sort obstruction

Two depth-blind canonical regimes were isolated:

\[
BR(x,y)=x_0,
\qquad
B0(x,y)=E_{cross}.
\]

They have the same locality/commutation shadow and residual reflection symmetry but different output sorts and re-entry behavior. Hence locality cannot select the output ontology.

Within `relation-only + base-valued`, `BR` is unique. Within `relation-only + singleton terminal event`, `B0` is unique. Across output policies, LC3 remains underdetermined.

### v0.3 — Two-Minimality Tradeoff and locality ceiling

Neither `BR` nor `B0` leaks radial endpoint information through its one-step mixed output: both are constant on the mixed domain.

But they optimize different notions of conservativity. With

\[
\kappa(F)=(c_{state}(F),c_{re}(F)),
\]

where `c_state` measures genuinely new output states/sorts and `c_re` measures immediate primitive re-entry,

\[
\boxed{\kappa(BR)=(0,1),\qquad \kappa(B0)=(1,0).}
\]

Thus:

- `BR` is uniquely minimal in **state-extension cost**: no new object or sort is required;
- `B0` is uniquely minimal in **primitive compositional cost** within the singleton-terminal regime: the new event is a sink unless an LC2 re-entry law is added.

The two cost vectors are incomparable. Therefore no unweighted locality/minimality principle chooses between them.

## Locality Ceiling Theorem

Within the audited FCOA-Z line, geometry-conditioned locality can determine **where** mixed commutation holds but cannot by itself determine:

1. the output sort of the mixed interaction;
2. whether the result re-enters the primitive operation;
3. how much radial information a richer law may retain.

Consequently:

\[
\boxed{\text{locality is a genuine compatibility principle, not a complete interaction-generation principle}.}
\]

Any further selection requires a new independent axiom about output typing, re-entry, information preservation, or universality. That is a cross-direction programme decision, not a further consequence of AQFT locality.

## Line Completion Gate verdict

- LC1: mixed cells are conservatively realizable;
- LC2: re-entry policy is a genuine selector variable;
- LC3: unique only after an output-policy class is fixed;
- dimension: both canonical locality regimes remain one-dimensional.

Therefore:

\[
\boxed{\texttt{1D-CLOSED}}
\]

with

\[
\boxed{\texttt{UNDERDETERMINED ACROSS OUTPUT POLICIES}}.
\]

There is no `DIMENSION-FORCING` result.

## Files

- [`SOL_LOCALITY_REPORT_v0_1.md`](./SOL_LOCALITY_REPORT_v0_1.md) — AQFT definitions, locality construction, orthogonal-category shadow, explicit FCOA realization, first underdetermination theorem and hostile audit.
- [`SOL_LOCALITY_SELECTOR_AUDIT_v0_2.md`](./SOL_LOCALITY_SELECTOR_AUDIT_v0_2.md) — relation-only selector theorem, `BR`/`B0` output-sort obstruction, locality-shadow collision, radial and automorphism selector no-go.
- [`SOL_LOCALITY_BR_VS_B0_AUDIT_v0_3.md`](./SOL_LOCALITY_BR_VS_B0_AUDIT_v0_3.md) — one-step information firewall, state-vs-reentry conservativity tradeoff, Two-Minimality Tradeoff Theorem and final Locality Ceiling Theorem.

## Publication decision

The SOL-LOCALITY package is mature enough for incorporation into an **Applied Directions synthesis**. A standalone AQFT/FCOA physical publication is not recommended: the strongest justified result is a formal orthogonality/locality embedding and a mathematically substantive no-go boundary, not a physical AQFT model.

## Closure rule

Do not reopen SOL-LOCALITY merely to enumerate more mixed formulas. Reopen only if another FCOA direction supplies a new independent principle capable of choosing output type, re-entry policy, or information-retention class. Otherwise the locality question is complete at the current FCOA-Z level.
