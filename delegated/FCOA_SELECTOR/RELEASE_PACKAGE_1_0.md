# SOL-SELECTOR v1.0 Release Package

Date: 2026-09-05
Status: PDF/LaTeX RELEASE PACKAGE BUILT AND VISUALLY AUDITED

## Publication title

English: `Selector Geometry for Mixed Extensions of a Reflected Partial Algebra`

Russian: `Геометрия селекторов смешанных расширений отражённой частичной алгебры`

Author:
- English/Zenodo: `Malachevsky, A.A.`
- Russian: `Малачевский А.А.`
- ORCID: `0009-0008-6009-3196`

## Built artifacts

The release package contains:

- `sol_selector_en.tex`
- `sol_selector_en.pdf`
- `sol_selector_ru.tex`
- `sol_selector_ru.pdf`
- `README.md`
- PDF preflight reports

Build engine: XeLaTeX.

Final rendered page counts:

- EN PDF: 8 pages
- RU PDF: 7 pages

Both PDFs passed render-based visual QA: no clipped text, broken glyphs, formula overflow, or page-layout defects were observed. PDF preflight confirms both files are openable, unencrypted, text-based, and contain no XFA forms.

## Frozen theorem nucleus

The release version freezes the one-step theorem package at

```math
M_0 \to F_{\to} \to F_{\mathrm{mix}} \to B0 \to BR,
```

with

```math
\theta_{span}\parallel\theta_{gap},
```

```math
\theta_{span}\wedge\theta_{gap}=\theta_{orb},
```

```math
\theta_{span}\vee\theta_{gap}=\theta_{phase}.
```

It also includes:

- uniqueness of `BR` as the reflection-compatible base-line anchor of `B0`;
- the incompatible-anchor no-common-upper-bound theorem;
- the exact weak/strong bifurcation boundary for `B0 -> BR`.

## Claim discipline

The release explicitly does not claim novelty for preservation-only partial-algebra homomorphisms, weak quotient constructions, free completions, or congruence lattices of `C2`/G-sets.

The contribution is scoped to the combined FCOA selector architecture, its canonically generated span/gap quotient geometry, and the transition from pure event quotient lattice behavior to constrained anchored-poset behavior.

No claim is made that span/gap/phase are uniformly parameter-free FO-definable in the weakest infinite reduct.

## DOI status

Zenodo DOI is intentionally not inserted yet. Add it only after the deposition record has been created.

## Research status

One-step `SOL-SELECTOR` is frozen for publication at v1.0.

Further research, if opened, should proceed under a separate re-entry line because re-entry changes the free object from terminal one-step events to term/tree closure.
