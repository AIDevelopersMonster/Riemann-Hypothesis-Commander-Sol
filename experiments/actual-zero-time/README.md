# RH-SOL-03 actual-zero-time experiment

Use actual zero ordinates `gamma_n` directly. Avoid smooth-time resampling as the primary test.

A basic matched component for a scalar observable `X_n` is:

`A_m = sum_n X_n exp(i * gamma_n * log(m))`.

Compare with smooth-time results only as a secondary diagnostic.
