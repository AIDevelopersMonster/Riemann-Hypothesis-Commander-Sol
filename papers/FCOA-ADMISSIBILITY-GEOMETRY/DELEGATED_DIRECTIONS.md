# Delegated FCOA Research Directions — Scientific Director Registry

**Scientific director:** main Commander Sol line  
**Purpose:** parallelize worthwhile side directions without allowing subordinate branches to steer the central programme by momentum alone.

## Governance principle

Delegated chats are **scientific supervisors of their own directions**, not co-equal directors of the FCOA programme.

A subordinate result enters the main line only after explicit review by the main scientific director.

The director will not adopt a result merely because a delegated branch produced a long development, a persuasive narrative, or a locally coherent programme.

## Upstream admission filter

A subordinate result is considered for the main line only if it satisfies most of the following:

1. mathematically correct with proof-level scope;
2. independently checked or hostile-audited when the claim is nontrivial;
3. materially sharper than what the main line already knows;
4. not a restatement of existing FCOA language;
5. does not silently import ordinary arithmetic or external labels;
6. comes with exact signature/sort assumptions;
7. survives the relevant Carrier-/Domain-/Value-Erasure distinction;
8. has explicit small-case checks and counterexample search where finite;
9. contributes to the strategic question of structural memory or its limits;
10. can be stated cleanly enough to justify the added complexity.

Interesting but peripheral results remain in their delegated branch.

## Central line retained by the director

The main line retains control of:

\[
\boxed{
G4\ \text{audit}
\longrightarrow
\text{bounded-output amplification}
\longrightarrow
\text{Arithmetic Leakage boundary}
}
\]

Arithmetic Leakage is not delegated as the primary programme because it is expected to become a central phase-boundary question for FCOA.

## Delegated direction A

**Git branch:** `director/fcoa-rigidity-cost`  
**Chat title:** `FCOA — SOL-RIGIDITY — Rigidity Cost & Skeleton Classification`

Mission: extremal cost of symmetry reduction, skeleton classification, subgroup realization, density/output/rigidity trade-offs.

Local supervisor reads `delegated/FCOA_RIGIDITY_COST/ROLE_AND_PROMPT.md` and `SOURCE_CONTEXT.md`.

## Delegated direction B

**Git branch:** `director/fcoa-infinite-memory`  
**Chat title:** `FCOA — SOL-INFINITY — Infinite Carrier & FO Memory Boundary`

Mission: infinite-carrier limit, FO versus stronger recoverability, rigorous boundary between local successor memory and global order memory.

Local supervisor reads `delegated/FCOA_INFINITE_MEMORY/ROLE_AND_PROMPT.md` and `SOURCE_CONTEXT.md`.

## Delegated direction C

**Git branch:** `director/fcoa-hybrid-memory`  
**Chat title:** `FCOA — SOL-HYBRID — Joint Memory of Partial Operations`

Mission: genuinely joint memory of two partial operations where neither reduct alone is rigid or sufficient; domain-domain, domain-value, and value-value synergy; strict Arithmetic Leakage firewall.

Local supervisor reads `delegated/FCOA_HYBRID_MEMORY/ROLE_AND_PROMPT.md` and `SOURCE_CONTEXT.md`.

## Delegated direction D

**Git branch:** `director/fcoa-branch-passport-lab`  
**Chat title:** `FCOA — SOL-PASSPORT — Invariant Lab & Branch Diff Auditor`

Mission: mandatory branch passports, automated invariant enumeration, exact branch diffs, counterexample search, reproducibility support. This direction is methods infrastructure, not a source of research priorities.

Local supervisor reads `delegated/FCOA_BRANCH_PASSPORT_LAB/ROLE_AND_PROMPT.md` and `SOURCE_CONTEXT.md`.

## Delegated direction E

**Git branch:** `director/fcoa-nesting-atomicity`  
**Chat title:** `FCOA — SOL-NESTING — Sandbox Atomicity & Composition Boundary`

Mission: formalize atomicity only relative to an explicit composition sandbox; separate isolation/indecomposability/atomicity; study how atomicity changes under operation/sort/erasure changes without importing classical prime-factorization assumptions.

Local supervisor reads `delegated/FCOA_NESTING_ATOMICITY/ROLE_AND_PROMPT.md` and `SOURCE_CONTEXT.md`.

## Reporting protocol

Every delegated supervisor should maintain an `UPSTREAM_MEMO.md` containing only results that they believe deserve director review.

The main director will periodically inspect these memos. Absence of upstream adoption does not imply a local result is wrong; it may simply be strategically peripheral.

No delegated branch may modify another delegated branch or the main FCOA files unless explicitly instructed.

## Interpretation of P2 recommendations

The director adopts from P2 the following methodological principles:

- FCOA is treated as a laboratory of structural memory, not as a replacement arithmetic;
- UNDEF is structural absence, not a value;
- domain, values, result sorts, anchors, and erasure are separate information channels;
- every branch should have a passport and a diff from its predecessor;
- atomicity must be sandbox-relative;
- new cells must justify exactly what symmetry/information they change.

The director does **not** adopt P2's suggestion that G3-A should automatically replace the central programme. G3-A is an important mechanism, but the main line has already moved to the stronger G4 bounded-output amplification question and then to the Arithmetic Leakage boundary.

## Mandatory publication rule — FCOA Foundation Definition 1.0

Effective 2026-08-29, **all five delegated directions and the central FCOA line** must use the foundational article

**Fixed-Carrier Oriented Algebra (FCOA): Definition, Typed Partial Operations, Carrier Erasure, and the Canonical M0 Baseline**, Version 1.0,

as the canonical citation for the meaning of the FCOA framework.

The authoritative rule is stored at:

`../FCOA-FOUNDATION-1.0/FOUNDATION_CITATION_DIRECTIVE.md`.

The Foundation DOI is currently pending Zenodo assignment. Once assigned, every new or still-unpublished FCOA manuscript must satisfy both release gates:

1. **Abstract/Аннотация:** explicitly identify the work as using the FCOA framework fixed by Definition 1.0 and print the Foundation DOI;
2. **Bibliography/Литература:** include the full Foundation bibliographic record with that DOI.

Every manuscript must also contain a short body paragraph identifying the exact FCOA object used: carrier, active/output/auxiliary sorts, primitive signature, changes relative to M0 or another cited baseline, erasure convention, and recovery/decomposition notion.

This instruction applies to `SOL-RIGIDITY`, `SOL-INFINITY`, `SOL-HYBRID`, `SOL-PASSPORT`, and `SOL-NESTING` even if a former working branch has already been deleted or archived: any **future revision or new publication** from that direction must comply.

Already published archival versions are not silently rewritten. A later Zenodo version/revision must add the Foundation citation.

DOI `10.5281/zenodo.22129787` remains a separate citation for the specific Admissibility Geometry `M0 -> G1 -> G2` theorem chain and does not replace the Foundation citation.