Axial plus split block theorem closed.

For profile (a,0)^r together with (P,Q)^n with even n and N=r+n, complete support is feasible iff a>=N-1 and P+min(Q,n-1)>=N-1.

In that regime the typed Pareto frontier is a parity segment on B_P+B_Q = r*a+n*(P+Q)-N*(N-1).

Witnesses: 6 separates square and triangular closure; 15 is a heterogeneous scalar collision with equal scalar boundary 3 but different typed Pareto frontiers.

Next target: two even split blocks.