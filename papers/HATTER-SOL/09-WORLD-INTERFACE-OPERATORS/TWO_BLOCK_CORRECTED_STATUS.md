Two-block complete-support problem is exactly an ordinary Lovasz (g,f)-factor problem on K_N with block-constant bounds g_j=max(0,N-1-Q_j), f_j=min(P_j,N-1). For two blocks, the ordinary Lovasz criterion reduces to a finite four-count check over d1,s1,d2,s2 because the residual graph of K_N is either empty or one connected component. The component correction is present only when every residual vertex is tight (g=f) and the residual parity is odd. The earlier blanket parity-factor wording was incorrect and is withdrawn. The exact witness 65 remains valid: square frontier {(4,0),(2,2),(0,4)}, triangular frontier {(7,0),(5,2)}. The canonical inter-world block alignment is by the original rational-prime labels p dividing n, not by individual irreducible vertices. Next target: classify admissible quadratic worlds before defining any world operator.