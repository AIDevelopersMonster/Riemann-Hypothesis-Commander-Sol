# H17 figures

Place the publication GTKWave screenshot here with the exact filename:

```text
H17_LAB03_WAVEFORM_26_CYCLES.png
```

Canonical repository path:

```text
papers/HATTER-SOL/17-NONABELIAN-TOMOGRAPHY-HARDWARE/figures/H17_LAB03_WAVEFORM_26_CYCLES.png
```

The file is referenced by `HATTER_SOL_17_RU_v1.1.md`.

Recommended crop: retain the full time axis and the signal rows from `clk` through `done`, including `state`, `op_idx`, `probe_idx`, `class_code`, `raw_signature`, `observed_signature`, `repaired_signature`, `fingerprint_valid`, and `status`.

Publication caption is stored in the article and in `docs/H17_09_SEQUENTIAL_LAB03_AND_WAVEFORM.md`.
