# H20-EXT-LAB-04 · Walsh spectrum under wheel + dyadic-density preserving randomization

Status: **PROTOCOL FROZEN BEFORE EXECUTION**

## 1. Motivation

LAB-03 found a finite spectral candidate:

- against wheel-210 preserving shuffles, prime Walsh degree mass at levels 1--2 is consistently larger than every five-seed baseline sample on validation widths W=12,13,14;
- cumulative mass through level 4 is consistently smaller;
- the same signs persist at W=15,16.

This passes the LAB-03 predeclared finite validation gate.

However the baseline still destroys a known nonstationarity of prime density:

[
Pr(n	ext{ prime})approxrac1{log n}.
]

A global shuffle inside one residue class modulo 210 mixes small and large integers and can therefore manufacture low-degree binary-digit differences, especially in high-order bits.

LAB-04 tests whether the LAB-03 signal survives after this density-gradient confound is controlled.

## 2. Frozen stratification

For every integer n>0 define its dyadic shell

[
J(n)=lfloorlog_2 nfloor.
]

Use a special shell for n=0.

For fixed width W, partition

[
{0,ldots,2^W-1}
]

by the joint key

[
oxed{(nmod210, J(n)).}
]

Within every nonempty joint stratum independently shuffle the prime labels.

Therefore the baseline preserves exactly:

1. the total number of primes;
2. the prime count in every residue class mod 210;
3. the prime count in every dyadic magnitude shell;
4. more strongly, the prime count in every joint wheel-210 x dyadic-shell cell.

It does not preserve the detailed binary pattern inside a shell/residue cell.

## 3. Spectral observer

Use exactly the LAB-03 exact integer Walsh-Hadamard transform and degree energies

[
E_{W,k}
=
sum_{|S|=k}widehat f_W(S)^2.
]

No observer definition is changed.

Primary diagnostics remain:

- nonconstant-renormalized TV distance;
- degree 1--2 cumulative mass;
- degree 1--4 cumulative mass;
- centroid;
- spectral entropy.

## 4. Seeds

Use the same frozen seeds:

[
1729,quad271828,quad314159,quad1618033,quad5772157.
]

## 5. Validation range

LAB-04 is a new falsification layer, so it freezes its own split before execution.

### Discovery

[
W=8,ldots,12.
]

### Validation

[
W=13,14,15.
]

### Extrapolation

[
W=16.
]

## 6. Falsification criterion

The LAB-03 spectral candidate is rejected as an independent prime-specific structural object if, under the joint wheel-210 + dyadic-shell preserving baseline:

1. the level-1--2 prime mass is no longer outside the five-seed baseline range on all validation widths; or
2. the level-1--4 sign pattern is no longer stable; or
3. the nonconstant spectral distance falls to ordinary seed-to-seed baseline scale.

A surviving candidate requires the same signed deviations on W=13,14,15 and no reversal at W=16.

## 7. Scientific interpretation

If the LAB-03 signal disappears, the correct interpretation is:

> the apparent low-degree spectral anomaly was generated by known small-prime residue structure plus the large-scale density gradient of primes.

If it survives, the remaining object becomes narrower:

> a finite binary-digit spectral deviation not explained by mod-210 counts or dyadic magnitude counts.

That still would not establish novelty.

## 8. Required artifacts

- `walsh_stratified_spectrum.csv`;
- `walsh_stratified_summary.json`;
- `H20_EXT_LAB04_REPORT.md`.

No curve fitting, FPGA synthesis or RH interpretation is permitted.
