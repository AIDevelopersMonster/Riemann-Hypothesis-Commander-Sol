# HATTER-SOL-16 · First A5 results

Exact certificate added in `certificates/a5_generating_pair_commutator_certificate.py`.

Current closed facts:

- 2280 ordered generating pairs in A5;
- 38 simultaneous-conjugacy orbits;
- generating commutators occur only in classes 3A, 5A, 5B;
- pair counts are 1080, 600, 600 respectively;
- one real 3-dimensional irreducible character separates all five conjugacy classes of A5;
- the fourth torus moment of the two-port operator contains the commutator trace term and therefore retains the H15 length-four mechanism beyond the dihedral case;
- a crude norm-tail estimate already gives rigorous scalar log-determinant separation of the three generating commutator classes for mu >= 58.

Next target: sharpen the large-mu bound toward the natural boundary mu=4 and then transport the observer to PSL(2,7).