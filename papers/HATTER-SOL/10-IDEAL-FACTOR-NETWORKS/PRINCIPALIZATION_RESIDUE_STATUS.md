Principalization residue layer closed.

For a principal input ideal A=prod p_j=(a), choose a minimal witness alpha_j at each node and define J_j=(alpha_j)p_j^{-1}. Then prod J_j is principal, say (beta), and |N(beta)|=prod delta(p_j), independent of witness choice.

The class group is therefore a conserved charge, not a third additive Pareto resource. What varies is the typed geometry Pi(beta) of the principalization residue.

In K=Q(sqrt(-15)), for (4)=p^2 pbar^2, different minimal companion choices give residue generators beta=4 and beta=omega^2=omega-4. Both have norm 16, but typed residue pairs are (4,0) and (3,1). Thus equal residue norm and equal ordinary network closure can hide different witness-level arithmetic geometry.

Correct architecture: residue is a fibre label over (B_P,B_Q), not a third scalar boundary coordinate.

Next target: multiplication/convolution law for residue fibres and a residue-refined response polynomial.