# HATTER-SOL-21 · SINGLE-SCALAR FIBER THEOREM

Status: **EXACT LOCAL THEOREM / RECIPROCAL SCALAR TARGET**

## 1. Fiber coordinate

Fix a nonexceptional local world at prime \(p\).

From H21-PG1–PG2,

\[
H_p=\langle x\rangle,
\qquad
h_p=e_p d_p,
\]

and

\[
\lambda_p=x^{e_p}\in\mathbf F_p^\times
\]

generates

\[
D_p=H_p\cap\mathbf F_p^\times.
\]

Every phase is uniquely

\[
m=r+k e_p,
\]

and

\[
x^m=\lambda_p^k x^r.
\]

Define the scalar orbit coordinate

\[
\boxed{
z_p(m)=\lambda_p^k\in D_p.
}
\]

## 2. Optional observer targets

Let one zero-bit be defined by a nonzero affine observer level

\[
\ell(y)=c,
\qquad
c\ne0.
\]

On projective phase \(r\),

\[
\ell(x^{r+k e_p})
=
\lambda_p^k\ell(x^r).
\]

If the bit is projectively supported, then

\[
\ell(x^r)\ne0
\]

and

\[
\boxed{
t_{\ell,c}(r)
=
\frac{c}{\ell(x^r)}
\in D_p.
}
\]

Hence

\[
\boxed{
\ell(x^{r+k e_p})=c
\iff
z_p(m)=t_{\ell,c}(r).
}
\]

Thus every optional zero-bit is exactly one equality test in the cyclic scalar
subgroup \(D_p\).

## Theorem H21-SF1 — single-scalar observer law

For fixed projective phase \(r\), let

- \(F(r)\) be the forced zero-bit mask;
- \(O(r)\) be the set of optional supported bits;
- \(t_j(r)\in D_p\) the scalar target of optional bit \(j\).

Then the complete local zero mask is

\[
\boxed{
Z(r,k)
=
F(r)
\cup
\{j\in O(r):\lambda_p^k=t_j(r)\}.
}
\]

Therefore all scalar-fiber dependence of the two-bit observer is carried by
the **single cyclic coordinate**

\[
\lambda_p^k.
\]

The observer does not have two independent scalar degrees of freedom.

## Corollary H21-SF2 — at most three masks on one ray

Because there are only two observer bits, as \(k\) ranges through the scalar
fiber the local mask assumes at most

\[
\boxed{1+|O(r)|\le3}
\]

distinct values, except that coincident optional targets merge two enriched
masks into one.

The default mask is \(F(r)\); each distinct optional target adds one enriched
mask value.

## Corollary H21-SF3 — optional-target collision criterion

Two optional bits \(0,1\) vanish simultaneously on the same scalar phase iff

\[
\boxed{
t_0(r)=t_1(r).
}
\]

But the two affine observer lines meet only at the declared prime-law target

\[
t_\sigma=
\begin{cases}
x,&\sigma=+1,\\
\tau(x),&\sigma=-1.
\end{cases}
\]

Therefore coincident optional targets can occur only on the distinguished
projective target ray

\[
\boxed{
r\equiv
\begin{cases}
+1\pmod{e_p},&\sigma=+1,\\
-1\pmod{e_p},&\sigma=-1,
\end{cases}
}
\]

provided the target belongs to \(H_p\).

Thus away from the distinguished ray, two optional bits cannot turn on
simultaneously.

## 3. Normalized scalar residual

Choose the discrete-log coordinate in \(D_p\)

\[
\lambda_p^{k_j^*(r)}=t_j(r).
\]

For optional bit \(j\), define

\[
\boxed{
u_{p,j}
=
k-k_j^*(r)
\pmod{d_p}.
}
\]

Then

\[
\boxed{
j\in Z
\iff
u_{p,j}=0.
}
\]

Equivalently, without choosing discrete logarithms, define the multiplicative
residual

\[
\boxed{
\rho_{p,j}
=
\frac{z_p(m)}{t_j(r)}
\in D_p.
}
\]

Then

\[
\boxed{
j\in Z
\iff
\rho_{p,j}=1.
}
\]

The additive and multiplicative residuals carry the same information.

## 4. Relation between two optional residuals

If two bits are optional on the same projective ray, then

\[
u_{p,0}-u_{p,1}
=
k_1^*(r)-k_0^*(r)
\pmod{d_p},
\]

which is independent of the selected scalar phase \(k\).

Equivalently,

\[
\boxed{
\frac{\rho_{p,0}}{\rho_{p,1}}
=
\frac{t_1(r)}{t_0(r)}
}
\]

is fixed by the projective ray.

Hence the two optional residuals are not independent random variables; they
are translates of a single scalar coordinate.

## 5. Reciprocal semiprime reduction

For \(n=pq\), after fixing character and projective observer codes, the two
directions reduce to scalar coordinates

\[
z_p=\lambda_p^{k_p}\in D_p,
\qquad
z_q=\lambda_q^{k_q}\in D_q.
\]

The remaining same-mask coupling is generated by whether these prime-selected
scalar coordinates hit their projective target sets.

Thus the unresolved H21 arithmetic has the exact form

\[
\boxed{
(q\bmod h_p,\ p\bmod h_q)
\longrightarrow
(z_p,z_q)
\longrightarrow
\text{target hits in }D_p\times D_q.
}
\]

## 6. Shared scalar modulus diagnostic

Let

\[
g_d=\gcd(d_p,d_q).
\]

When a direction has one optional target, use the additive normalized residual

\[
u_p\in\mathbf Z/d_p\mathbf Z,
\qquad
u_q\in\mathbf Z/d_q\mathbf Z.
\]

Their reductions modulo \(g_d\) permit exact cross-field comparisons:

\[
\bar u_p=u_p\bmod g_d,
\qquad
\bar u_q=u_q\bmod g_d.
\]

Candidate reciprocal relations include

\[
\bar u_p=\bar u_q,
\qquad
\bar u_p=-\bar u_q.
\]

These are exploratory diagnostics, not theorems.

## 7. Publication boundary

H21-SF1–SF3 are exact consequences of the projective/scalar orbit geometry.

They materially compress the local observer, but publication significance
depends on whether reciprocal prime sampling of the normalized scalar
residuals obeys a nontrivial arithmetic law beyond generic cyclic-group
coordinates.
